---
phase: 12-testing-and-llm-quality-review
task: 1
total_tasks: 6
status: not_started
last_updated: 2026-05-20T15:18:36.218Z
---

<current_state>
Phase 12 (Testing + LLM Quality Review) is freshly added to the roadmap with full Goal / Depends / Success Criteria, scoped from a Q&A turn with the user. The two tracks are:

  Track A — deterministic test suite (Vitest + Playwright + MSW + supertest)
  Track B — nightly Claude-driven ICP funnel simulation (3-5 personas walk
            the full product, vision API judges each step, chain-of-critique
            produces ranked friction reports under .planning/qa-reports/)

Both advisory (never block merges). Budget-capped via `CLAUDE_QA_MAX_USD`.

Track B MVP is the next thing to implement. We agreed on a 3-step launch:
  (1) personas.json (5 ICPs)
  (2) install playwright + @anthropic-ai/sdk
  (3) landing-page-only MVP harness, expand surface in follow-ups

The previous session hit 78% context before any Track B code landed; the
user asked to pause and start fresh. **Working tree is clean. 12 commits
ahead of upstream main.**
</current_state>

<completed_work>

This session shipped (and committed) a lot before pausing:

- Phase 6 (curriculum content module + paused-phase scaffolding) — `832d1f5`
- Phase 7 (reader toolbar + progress strip + mark-complete) — `022c541`
- Phase 8 (WebVM iframe panel + postMessage drill-pass) — `31a4a85`
- Phase 9 (premium bonus library /bonus + landing page redesign by `octo:droids:octo-frontend-developer`) — `ce4985a`
- /signup page (landing CTA was dead-ended) — `a157450`
- /pricing rewrite ($0 Free / $30/mo Pro, removed nested main bug) — `6f1cddf`
- Roadmap phases 10+11 added (email engagement track) — `59f2633`
- Phase 10 (curriculum drip — EmailDispatch model, Drip.tsx template, /api/cron/drip route, /api/unsubscribe handler) — `135b113`
- Phase 10 gaps closed (signup timezone capture + requireSession lastActiveAt touch) — `43e0e75`
- Phase 12 added scope-gated — `4c934c3`
- Phase 12 scope locked from user Q&A — `309e01e`

App builds clean: `npm run build` produces 14 routes including `/api/cron/drip`, `/api/unsubscribe`, `/bonus`, `/bonus/[slug]`, `/signup`, `/`. No uncommitted work.
</completed_work>

<remaining_work>

Track B MVP (in order):

1. **Personas** — write `app/scripts/qa/personas.json` with 5 ICPs:
   - **Sarah** (32, career-switcher, bootcamp grad, 6 months job-hunting, $30/mo budget tight, skeptical of cert-mill courses, wants proof she'll pass)
   - **Marcus** (28, SOC L1, Security+ holder, manager mandated CEH, 2h/week, wants drill-style efficient prep not theory)
   - **Alex** (24, bootcamp grad, no industry job yet, cynical about training-industrial-complex, $30 is "I'll think about it")
   - **Priya** (27, software engineer, HTB regular, doing CEH for fun+career pivot, wants advanced content, will judge harshly if lessons feel "Udemy-watered-down")
   - **Dave** (41, IT manager mandated by CISO, no passion for hacking, will pay $30/mo without blinking, wants minimum-time-to-pass)

   Each persona needs: name, age, role, situation, pain_points, what_would_convert, what_would_make_them_bail, voice.

2. **Install deps** — `npm install --save-dev playwright @anthropic-ai/sdk` then `npx playwright install chromium` (~250 MB download, flag this to user before running).

3. **MVP harness** — `app/scripts/qa/icp-funnel-sim.ts`:
   - Reads ANTHROPIC_API_KEY from env (use claude-api skill best practices: prompt caching for static persona context)
   - Boots Playwright headless against http://localhost:3000
   - For each persona: navigates to `/`, captures 3 screenshots (hero, mid-scroll, footer)
   - Sends screenshots + persona context to Claude Sonnet vision asking: first-3-second impression, animations/motion judgment, headline clarity, trust signals, would-they-click-CTA, what would make them bail
   - Chain-of-critique: second pass re-reads its own notes via Haiku (cheaper, no vision needed)
   - Writes `.planning/qa-reports/YYYY-MM-DD-landing.md` with per-persona walkthrough + conversion verdict + ranked friction points
   - Budget tracker — count tokens × pricing, halt if `CLAUDE_QA_MAX_USD` exceeded (default $2/run)
   - Use Sonnet (`claude-sonnet-4-6` or latest) for vision pass; Haiku (`claude-haiku-4-5-20251001`) for synthesis pass

4. **Wire `npm run qa:icp`** in `app/package.json` scripts.

5. **README** in `app/scripts/qa/README.md` documenting: how to run, what env vars are needed, what cost to expect.

6. **Expand surface** (follow-up tasks, not this session):
   - signup flow → screenshot the form, simulate filling it (or skip and judge by UX), critique
   - curriculum days 1, 3, 10, 13 (the ones with drills) — read lesson, attempt quiz, judge if persona learned
   - WebVM lab via iframe — out-of-scope unless we have the persona "click Run this drill" + verify the iframe loads
   - Exam simulator (Day 14)

Track A (deterministic Vitest+Playwright+MSW+supertest suite) is **deferred** until Track B MVP proves the harness shape.
</remaining_work>

<decisions_made>

- **Test stack**: Vitest + Playwright + MSW + supertest (user chose "with mocking")
- **LLM provider**: Claude API direct via `@anthropic-ai/sdk` (user chose Claude over Copilot Workspace; we own prompt + budget)
- **Trigger**: nightly whole-repo at 03:00 UTC, NOT per-PR (user chose whole-repo)
- **Severity**: advisory only, never blocks merges (user chose advisory)
- **Self-review**: chain-of-critique multiple passes (user chose chain over single-pass)
- **Phase 12 split**: Track A (deterministic tests) + Track B (ICP simulation). Track B first because user's detailed scoping makes it the headline value.
- **MVP shape**: landing-page-only first, expand surface phase-by-phase. Avoid building wrong abstraction.
- **Models**: Sonnet for per-step vision pass; Haiku for chain-of-critique synthesis.
- **Budget**: `CLAUDE_QA_MAX_USD=2` default per invocation. ~$30/mo at 3 personas nightly.
</decisions_made>

<blockers>
None blocking. Two soft asks for the next session:
1. User to provide ANTHROPIC_API_KEY env var before the harness can actually run (script will exit cleanly without it; build/typecheck is unaffected).
2. ~250 MB Chromium download is the longest step in the deps install — flag it before running.
</blockers>

<context>
Strategic context the next session needs:

- The product (CEH Prep) is a 14-day CEH v13 prep SaaS, $30/mo Pro, 3 days free. App is at /Users/saharbarak/personal/ceh-prep/app/. WebVM lab fork lives at /Users/saharbarak/personal/ceh-webvm/ and deploys to https://saharbarak.github.io/ceh-webvm/.
- The landing page was just redesigned by the `octo:droids:octo-frontend-developer` agent — it sells on receipts (real counts of days/questions/articles/drills), not generic claims. Three preview cards pull actual bonus items. Mock terminal section with blinking caret. The landing is what Track B's MVP critiques first.
- Phase 10 (drip email) just shipped — every new signup will eventually receive 14 days of curriculum-tied emails. Track B's harness can verify the signup form respects the timezone field.
- The user's design taste is strict: Cabinet Grotesk + Satoshi + JetBrains Mono, lime accent on zinc-950, asymmetric layouts, NO Inter/AI-purple/centered-hero-cliché. Track B personas should be prompted to judge the landing against the user's actual taste.
- claude-api skill is available — invoke it before writing the Anthropic SDK code to get current prompt caching best practices baked in.
- Memory files at ~/.claude/projects/-Users-saharbarak-personal-ceh-prep/memory/ already capture: premium-bonus-content, whatsapp-content-pipeline, webvm-fork. These have all the surrounding context.
</context>

<next_action>
Open a fresh session. Read this file + .planning/HANDOFF.json. Begin with task #1: write `app/scripts/qa/personas.json` with the 5 ICPs (backstories above). After personas land, run the deps install and warn the user about the ~250 MB Chromium download. Then write the MVP harness — use the `claude-api` skill via the Skill tool to pull in current best practices for prompt caching before the first line of SDK code.
</next_action>
