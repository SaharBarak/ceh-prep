// Stub for the `server-only` import used in src/lib/infra/* — under
// Vitest we don't have the Next bundler boundary check, so we
// substitute an empty module via the resolve alias in vitest.config.
export {};
